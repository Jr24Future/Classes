/*
document.addEventListener('DOMContentLoaded', () => {
    fetch('software-info.json')
      .then(response => response.json())
      .then(data => {
        document.getElementById('software-title').textContent = data.title;
        document.getElementById('software-description').textContent = data.description;
        document.getElementById('software-additional-info').textContent = data.additionalInfo;
      })
      .catch(error => console.error('Error loading software information:', error));
  });
  */